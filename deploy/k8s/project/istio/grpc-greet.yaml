apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: grpc-greet
spec:
  hosts:
    - grpc-greet
  http:
    - route:
        - destination:
            host: grpc-greet
            subset: v1
---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: grpc-greet
spec:
  host: grpc-greet
  subsets:
    - name: v1
      labels:
        version: v1
    - name: v2
      labels:
        version: v2
    - name: v3
      labels:
        version: v3
