apiVersion: apps/v1
kind: Deployment
metadata:
  name: grpc-greet-v1
  labels:
    repo: gola
    app: grpc-greet
    version: v1
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      repo: gola
      app: grpc-greet
      version: v1
  template:
    metadata:
      labels:
        repo: gola
        app: grpc-greet
        version: v1
    spec:
      containers:
        - name: server
          image: registry:5000/gola/web:v1
          command:
            - ./gola
          args:
            - grpc
            - greet
          env:
            - name: APP_SITE
              value: grpc-greet
          envFrom:
            - configMapRef:
                name: env-prod
          resources:
            limits:
              memory: "128Mi"
              cpu: "50m"
          ports:
            - containerPort: 50051

---
apiVersion: v1
kind: Service
metadata:
  name: grpc-greet
  labels:
    repo: gola
    app: grpc-greet
spec:
  selector:
    repo: gola
    app: grpc-greet
  ports:
    - port: 50051
      targetPort: 50051
      name: http2
